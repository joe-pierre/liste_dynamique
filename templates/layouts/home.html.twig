{% extends 'layouts/base.html.twig' %}

{% block title 'Liste Dynamique'%}

{% block body %}
	<section class="mt-5 container">

	
		<div class="row">
			<div class="col-md-6 mx-auto">
				<h1 class="mb-5 h2 text-center">Are you facing any <span class="text-danger">issues</span>  😌?</h1>
				
				{{form_start(form)}}
					{{form_widget(form)}}
					<button type="submit" class="btn btn-primary btn-lg btn-block" formnovalidate>Send</button>
				{{form_end(form)}}
			</div>

		</div>

	</section>
	
{% endblock %}
