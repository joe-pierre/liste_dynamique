{% extends 'layouts/base.html.twig' %}

{% block title 'Liste Dynamique'%}

{% block body %}
	<section class="mt-5 container">

		<h1 class="mb-5 text-center">Are you facing any issues ? 😥</h1>
	
		<div class="row">

			<div class="col-md-6 mx-auto">
				{{form_start(form)}}
				{{form_widget(form)}}
				<button type="submit" class="btn btn-primary btn-lg btn-block">Send</button>
				{{form_end(form)}}
			</div>

		</div>

	</section>
	
{% endblock %}
